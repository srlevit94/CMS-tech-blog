{{#each project as |projects| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2> {{project.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{project.description}}</h3>
  </div>
</div>
{{/each}}