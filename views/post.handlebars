<section class="container">
  <h3 class="container">All Posts</h3>
  <h4>Opening Hour:</h4>
  <section class="flex-row justify-center">
    {{#each posts as |post|}}
    {{> post-details}}
    {{/each}}
  </section>
</section>