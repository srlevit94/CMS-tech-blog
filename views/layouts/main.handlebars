<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
</head>

<body>
  
  <header class="display-flex justify-space-between align-center p-2">
    <h1>
      Tech Blog
    </h1>
    
    <nav>
      <button class="btn" id="home" href="/">Home</button>
      {{#if logged_in}}
        <button class="btn" id="dashboard" href="./dashboard">Dashboard</button>
        <button class="btn" id="logout" href="./logout">Logout</button>
        {{else}}
        <button class="btn" id="dashboard" href="./login">Dashboard</button>
        <button class="btn" id="login" href="./login">Login</button>
        {{/if}}
    </nav>
    
  </header>

  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
      
  </footer>

  <!-- TODO: Add a comment describing the functionality of this helper -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
